'use client';

import { useActionState, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { Button, Input } from '@/components/ui';
import { FormMessage, SubmitButton } from '@/components/form-controls';
import { initialFormState, type FormState } from '@/lib/actions';

type Profile = { first_name: string | null; last_name: string | null } | { first_name: string | null; last_name: string | null }[] | null;
type Options = { customers: { id: string; name: string }[]; objects: { id: string; customer_id: string; name: string }[]; jobs: { id: string; customer_id: string; cleaning_object_id: string; title: string; scheduled_date: string }[]; employees: { id: string; profiles: Profile }[] };
type Complaint = { id?: string; customer_id?: string; cleaning_object_id?: string; job_id?: string | null; title?: string; description?: string; priority?: string; status?: string; assigned_member_id?: string | null; due_date?: string | null; internal_note?: string | null };
type Action = (state: FormState, formData: FormData) => Promise<FormState>;
function first<T>(value: T | T[] | null) { return Array.isArray(value) ? value[0] ?? null : value; }
function name(profile: Profile) { const person = first(profile); return [person?.first_name, person?.last_name].filter(Boolean).join(' ') || 'Mitarbeiter'; }

export function ComplaintForm({ complaint, options, action, submitLabel }: { complaint?: Complaint; options: Options; action: Action; submitLabel: string }) {
  const [state, formAction] = useActionState(action, initialFormState); const router = useRouter();
  useEffect(() => { if (state.status === 'success' && state.id) router.push(`/dashboard/reklamationen/${state.id}?success=${encodeURIComponent('Reklamation wurde gespeichert.')}`); }, [router, state]);
  return <form action={formAction} className="space-y-6"><FormMessage status={state.status} message={state.message} />
    <div className="grid gap-5 sm:grid-cols-2"><label className="block text-sm font-medium">Kunde <span className="text-red-700">*</span><select className="mt-1.5 h-10 w-full rounded-md border bg-white px-3 text-sm" name="customer_id" defaultValue={complaint?.customer_id ?? ''} required><option value="" disabled>Kunde auswaehlen</option>{options.customers.map((customer) => <option key={customer.id} value={customer.id}>{customer.name}</option>)}</select></label><label className="block text-sm font-medium">Objekt <span className="text-red-700">*</span><select className="mt-1.5 h-10 w-full rounded-md border bg-white px-3 text-sm" name="cleaning_object_id" defaultValue={complaint?.cleaning_object_id ?? ''} required><option value="" disabled>Objekt auswaehlen</option>{options.objects.map((object) => <option key={object.id} value={object.id}>{object.name}</option>)}</select></label><label className="block text-sm font-medium sm:col-span-2">Auftrag <select className="mt-1.5 h-10 w-full rounded-md border bg-white px-3 text-sm" name="job_id" defaultValue={complaint?.job_id ?? ''}><option value="">Kein Auftrag verknuepft</option>{options.jobs.map((job) => <option key={job.id} value={job.id}>{job.scheduled_date}: {job.title}</option>)}</select></label><label className="block text-sm font-medium sm:col-span-2">Titel <span className="text-red-700">*</span><Input className="mt-1.5" name="title" defaultValue={complaint?.title ?? ''} maxLength={160} required /></label><label className="block text-sm font-medium sm:col-span-2">Beschreibung <span className="text-red-700">*</span><textarea className="mt-1.5 min-h-32 w-full rounded-md border bg-white px-3 py-2 text-sm" name="description" defaultValue={complaint?.description ?? ''} maxLength={4000} required /></label><label className="block text-sm font-medium">Prioritaet<select className="mt-1.5 h-10 w-full rounded-md border bg-white px-3 text-sm" name="priority" defaultValue={complaint?.priority ?? 'NORMAL'}><option value="LOW">Niedrig</option><option value="NORMAL">Normal</option><option value="HIGH">Hoch</option><option value="URGENT">Dringend</option></select></label><label className="block text-sm font-medium">Status<select className="mt-1.5 h-10 w-full rounded-md border bg-white px-3 text-sm" name="status" defaultValue={complaint?.status ?? 'OPEN'}><option value="OPEN">Offen</option><option value="IN_PROGRESS">In Bearbeitung</option><option value="RESOLVED">Geloest</option><option value="CLOSED">Geschlossen</option></select></label><label className="block text-sm font-medium">Zugewiesener Mitarbeiter<select className="mt-1.5 h-10 w-full rounded-md border bg-white px-3 text-sm" name="assigned_member_id" defaultValue={complaint?.assigned_member_id ?? ''}><option value="">Nicht zugewiesen</option>{options.employees.map((employee) => <option key={employee.id} value={employee.id}>{name(employee.profiles)}</option>)}</select></label><label className="block text-sm font-medium">Faellig am<Input className="mt-1.5" name="due_date" type="date" defaultValue={complaint?.due_date ?? ''} /></label><label className="block text-sm font-medium sm:col-span-2">Interne Notiz<textarea className="mt-1.5 min-h-24 w-full rounded-md border bg-white px-3 py-2 text-sm" name="internal_note" defaultValue={complaint?.internal_note ?? ''} maxLength={4000} /></label></div>
    <div className="flex justify-end gap-3"><Button type="button" variant="outline" onClick={() => router.back()}>Abbrechen</Button><SubmitButton>{submitLabel}</SubmitButton></div>
  </form>;
}
