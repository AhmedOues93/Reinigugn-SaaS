'use client';

import { useActionState, useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { Pencil } from 'lucide-react';
import { Button, Input } from '@/components/ui';
import { FormMessage, SubmitButton } from '@/components/form-controls';
import { initialFormState, type FormState } from '@/lib/actions';

type Profile = { first_name: string | null; last_name: string | null } | { first_name: string | null; last_name: string | null }[] | null;
type Options = { customers: { id: string; name: string }[]; objects: { id: string; customer_id: string; name: string }[]; jobs: { id: string; customer_id: string; cleaning_object_id: string; title: string; scheduled_date: string }[]; employees: { id: string; profiles: Profile }[] };
type Complaint = { id?: string; customer_id?: string; cleaning_object_id?: string; job_id?: string | null; title?: string; description?: string; priority?: string; status?: string; assigned_member_id?: string | null; due_date?: string | null; internal_note?: string | null };
type Action = (state: FormState, formData: FormData) => Promise<FormState>;
function first<T>(value: T | T[] | null) { return Array.isArray(value) ? value[0] ?? null : value; }
function name(profile: Profile) { const person = first(profile); return [person?.first_name, person?.last_name].filter(Boolean).join(' ') || 'Mitarbeiter'; }

export function ComplaintForm({ complaint, options, action, submitLabel }: { complaint?: Complaint; options: Options; action: Action; submitLabel: string }) {
  const [state, formAction] = useActionState(action, initialFormState); const router = useRouter(); const [editing, setEditing] = useState(!complaint?.id);
  useEffect(() => { if (state.status === 'success' && state.id) { if (complaint?.id) { setEditing(false); router.refresh(); } else router.push(`/dashboard/reklamationen/${state.id}?success=${encodeURIComponent('Reklamation wurde gespeichert.')}`); } }, [complaint?.id, router, state]);
  if (complaint?.id && !editing) {
    const customer = options.customers.find((item) => item.id === complaint.customer_id);
    const object = options.objects.find((item) => item.id === complaint.cleaning_object_id);
    const employee = options.employees.find((item) => item.id === complaint.assigned_member_id);
    const statusLabel: Record<string, string> = { OPEN: 'Offen', IN_PROGRESS: 'In Bearbeitung', RESOLVED: 'Gelöst', CLOSED: 'Geschlossen' };
    const priorityLabel: Record<string, string> = { LOW: 'Niedrig', NORMAL: 'Normal', HIGH: 'Hoch', URGENT: 'Dringend' };
    return <div className="space-y-5"><FormMessage status={state.status} message={state.message} /><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-semibold">Vorgang</p><p className="mt-1 text-sm text-muted-foreground">Gespeicherte Reklamationsdaten.</p></div><Button type="button" variant="outline" onClick={() => setEditing(true)}><Pencil className="size-4" />Bearbeiten</Button></div><dl className="grid gap-x-8 gap-y-4 sm:grid-cols-2"><div><dt className="text-xs text-muted-foreground">Kunde</dt><dd className="mt-1 font-medium">{customer?.name ?? '—'}</dd></div><div><dt className="text-xs text-muted-foreground">Objekt</dt><dd className="mt-1 font-medium">{object?.name ?? '—'}</dd></div><div><dt className="text-xs text-muted-foreground">Priorität</dt><dd className="mt-1">{priorityLabel[complaint.priority ?? 'NORMAL'] ?? complaint.priority}</dd></div><div><dt className="text-xs text-muted-foreground">Status</dt><dd className="mt-1">{statusLabel[complaint.status ?? 'OPEN'] ?? complaint.status}</dd></div><div><dt className="text-xs text-muted-foreground">Zugewiesen</dt><dd className="mt-1">{employee ? name(employee.profiles) : 'Nicht zugewiesen'}</dd></div><div><dt className="text-xs text-muted-foreground">Fällig</dt><dd className="mt-1">{complaint.due_date || '—'}</dd></div><div className="sm:col-span-2"><dt className="text-xs text-muted-foreground">Beschreibung</dt><dd className="mt-1 whitespace-pre-wrap text-sm leading-6">{complaint.description || '—'}</dd></div>{complaint.internal_note && <div className="sm:col-span-2"><dt className="text-xs text-muted-foreground">Interne Notiz</dt><dd className="mt-1 whitespace-pre-wrap text-sm leading-6">{complaint.internal_note}</dd></div>}</dl></div>;
  }

  return <form action={formAction} className="space-y-6"><FormMessage status={state.status} message={state.message} />
    <div className="grid gap-5 sm:grid-cols-2"><label className="block text-sm font-medium">Kunde <span className="text-danger">*</span><select className="mt-1.5 min-h-touch w-full rounded-md border bg-card px-3 text-sm" name="customer_id" defaultValue={complaint?.customer_id ?? ''} required><option value="" disabled>Kunde auswählen</option>{options.customers.map((customer) => <option key={customer.id} value={customer.id}>{customer.name}</option>)}</select></label><label className="block text-sm font-medium">Objekt <span className="text-danger">*</span><select className="mt-1.5 min-h-touch w-full rounded-md border bg-card px-3 text-sm" name="cleaning_object_id" defaultValue={complaint?.cleaning_object_id ?? ''} required><option value="" disabled>Objekt auswählen</option>{options.objects.map((object) => <option key={object.id} value={object.id}>{object.name}</option>)}</select></label><label className="block text-sm font-medium sm:col-span-2">Auftrag <select className="mt-1.5 min-h-touch w-full rounded-md border bg-card px-3 text-sm" name="job_id" defaultValue={complaint?.job_id ?? ''}><option value="">Kein Auftrag verknüpft</option>{options.jobs.map((job) => <option key={job.id} value={job.id}>{job.scheduled_date}: {job.title}</option>)}</select></label><label className="block text-sm font-medium sm:col-span-2">Titel <span className="text-danger">*</span><Input className="mt-1.5" name="title" defaultValue={complaint?.title ?? ''} maxLength={160} required /></label><label className="block text-sm font-medium sm:col-span-2">Beschreibung <span className="text-danger">*</span><textarea className="mt-1.5 min-h-32 w-full rounded-md border bg-card px-3 py-2 text-sm" name="description" defaultValue={complaint?.description ?? ''} maxLength={4000} required /></label><label className="block text-sm font-medium">Priorität<select className="mt-1.5 min-h-touch w-full rounded-md border bg-card px-3 text-sm" name="priority" defaultValue={complaint?.priority ?? 'NORMAL'}><option value="LOW">Niedrig</option><option value="NORMAL">Normal</option><option value="HIGH">Hoch</option><option value="URGENT">Dringend</option></select></label><label className="block text-sm font-medium">Status<select className="mt-1.5 min-h-touch w-full rounded-md border bg-card px-3 text-sm" name="status" defaultValue={complaint?.status ?? 'OPEN'}><option value="OPEN">Offen</option><option value="IN_PROGRESS">In Bearbeitung</option><option value="RESOLVED">Gelöst</option><option value="CLOSED">Geschlossen</option></select></label><label className="block text-sm font-medium">Zugewiesener Mitarbeiter<select className="mt-1.5 min-h-touch w-full rounded-md border bg-card px-3 text-sm" name="assigned_member_id" defaultValue={complaint?.assigned_member_id ?? ''}><option value="">Nicht zugewiesen</option>{options.employees.map((employee) => <option key={employee.id} value={employee.id}>{name(employee.profiles)}</option>)}</select></label><label className="block text-sm font-medium">Fällig am<Input className="mt-1.5" name="due_date" type="date" defaultValue={complaint?.due_date ?? ''} /></label><label className="block text-sm font-medium sm:col-span-2">Interne Notiz<textarea className="mt-1.5 min-h-24 w-full rounded-md border bg-card px-3 py-2 text-sm" name="internal_note" defaultValue={complaint?.internal_note ?? ''} maxLength={4000} /></label></div>
    <div className="flex justify-end gap-3"><Button type="button" variant="outline" onClick={() => router.back()}>Abbrechen</Button><SubmitButton>{submitLabel}</SubmitButton></div>
  </form>;
}
