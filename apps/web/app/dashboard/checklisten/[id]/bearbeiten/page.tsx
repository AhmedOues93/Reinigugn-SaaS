import { notFound } from 'next/navigation'; import { ChecklistTemplateForm } from '@/components/checklist-template-form'; import { getChecklistTemplate } from '@/lib/data/checklists'; import { updateChecklistTemplate } from '../../actions'; export default async function Edit({ params }: { params: Promise<{id:string}> }) { const t=await getChecklistTemplate((await params).id); if(!t) notFound(); return <div className="mx-auto max-w-3xl"><h1 className="mb-6 text-2xl font-semibold">Checkliste bearbeiten</h1><ChecklistTemplateForm template={t} action={updateChecklistTemplate.bind(null,t.id)} /></div>; }
