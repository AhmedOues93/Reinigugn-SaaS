import { notFound } from 'next/navigation';
import { JobForm } from '@/components/job-form'; import { Card } from '@/components/ui'; import { getJob, listActiveEmployeeOptions } from '@/lib/data/jobs'; import { listCustomerOptions } from '@/lib/data/customers'; import { listCleaningObjectOptions } from '@/lib/data/cleaning-objects'; import { listActiveChecklistTemplateOptions } from '@/lib/data/checklists'; import { updateJob } from '../../actions';
export default async function EditJobPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const [job, customers, objects, employees, templates] = await Promise.all([getJob(id), listCustomerOptions(), listCleaningObjectOptions(), listActiveEmployeeOptions(), listActiveChecklistTemplateOptions()]); if (!job) notFound(); return <div className="mx-auto max-w-3xl"><h1 className="text-2xl font-semibold">Auftrag bearbeiten</h1><Card className="mt-7 p-5 sm:p-7"><JobForm job={job} customers={customers} objects={objects} employees={employees} templates={templates} action={updateJob.bind(null, job.id)} submitLabel="Aenderungen speichern" /></Card></div>; }
