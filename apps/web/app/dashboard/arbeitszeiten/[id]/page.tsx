import { notFound } from 'next/navigation'; import { Card } from '@/components/ui'; import { getTimeEntry } from '@/lib/data/time-entries'; import { TimeCorrectionForm } from '@/components/time-correction-form'; import { correctTimeEntry } from '../actions';
const f = (v: string | null) => v ? new Intl.DateTimeFormat('de-DE', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(v)) : 'Laeuft';
export default async function TimeEntryDetail({ params }: { params: Promise<{ id: string }> }) { const entry = await getTimeEntry((await params).id); if (!entry) notFound(); const job = Array.isArray(entry.jobs) ? entry.jobs[0] : entry.jobs; const member = Array.isArray(entry.company_members) ? entry.company_members[0] : entry.company_members; const profile = Array.isArray(member?.profiles) ? member.profiles[0] : member?.profiles; const audits = entry.time_entry_audit_logs ?? []; return <div className="mx-auto max-w-4xl"><h1 className="text-2xl font-semibold">Arbeitszeit</h1><Card className="mt-6 p-6"><dl className="grid gap-5 sm:grid-cols-2 text-sm"><div><dt className="text-slate-500">Mitarbeiter</dt><dd>{profile?.first_name} {profile?.last_name}</dd></div><div><dt className="text-slate-500">Auftrag</dt><dd>{job?.title}</dd></div><div><dt className="text-slate-500">Kunde / Objekt</dt><dd>{job?.customers?.[0]?.name} / {job?.cleaning_objects?.[0]?.name}</dd></div><div><dt className="text-slate-500">Geplanter Zeitraum</dt><dd>{f(job?.planned_start_at ?? null)} bis {f(job?.planned_end_at ?? null)}</dd></div><div><dt className="text-slate-500">Tatsaechlich</dt><dd>{f(entry.started_at)} bis {f(entry.finished_at)}</dd></div><div><dt className="text-slate-500">Dauer / Status</dt><dd>{entry.duration_minutes == null ? 'Laeuft' : `${Math.floor(entry.duration_minutes / 60)} h ${entry.duration_minutes % 60} min`} · {entry.finished_at ? 'Beendet' : 'Laeuft'}</dd></div><div><dt className="text-slate-500">Quelle</dt><dd>{entry.start_source === 'MANUAL' ? 'Manuell' : 'App'}</dd></div><div><dt className="text-slate-500">Korrekturstatus</dt><dd>{audits.length ? 'Korrigiert' : 'Original'}</dd></div></dl><TimeCorrectionForm action={correctTimeEntry.bind(null, entry.id)} startedAt={entry.started_at} finishedAt={entry.finished_at} /></Card><Card className="mt-5 p-6"><h2 className="font-semibold">Korrekturhistorie</h2>{audits.length === 0 ? <p className="mt-3 text-sm text-slate-600">Keine Korrekturen vorhanden.</p> : <div className="mt-4 space-y-4">{audits.map((audit) => { const actor = Array.isArray(audit.profiles) ? audit.profiles[0] : audit.profiles; return <div key={audit.id} className="border-t pt-4 text-sm"><p className="font-medium">Geaendert von: {actor?.first_name} {actor?.last_name} · {f(audit.changed_at)}</p><p className="mt-1">Vorheriger Start/Ende: {f(audit.previous_started_at)} bis {f(audit.previous_finished_at)}</p><p>Neuer Start/Ende: {f(audit.new_started_at)} bis {f(audit.new_finished_at)}</p><p className="mt-1 text-slate-600">Korrekturgrund: {audit.reason}</p></div>; })}</div>}</Card></div>; }
