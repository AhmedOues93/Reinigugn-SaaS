import { notFound } from 'next/navigation'; import { ScheduleForm } from '@/components/schedule-form'; import { Card } from '@/components/ui'; import { getServiceSchedule, listActiveEmployeeOptions } from '@/lib/data/jobs'; import { listCustomerOptions } from '@/lib/data/customers'; import { listCleaningObjectOptions } from '@/lib/data/cleaning-objects'; import { updateServiceSchedule } from '../../../actions';
export default async function EditSchedulePage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const [schedule, customers, objects, employees] = await Promise.all([getServiceSchedule(id), listCustomerOptions(), listCleaningObjectOptions(), listActiveEmployeeOptions()]); if (!schedule) notFound(); return <div className="mx-auto max-w-3xl"><h1 className="text-2xl font-semibold">Plan bearbeiten</h1><Card className="mt-7 p-5 sm:p-7"><ScheduleForm schedule={schedule} customers={customers} objects={objects} employees={employees} action={updateServiceSchedule.bind(null, id)} submitLabel="Aenderungen speichern" /></Card></div>; }
