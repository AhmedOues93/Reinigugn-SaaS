import Link from 'next/link'; import { notFound } from 'next/navigation'; import { Pencil } from 'lucide-react'; import { getServiceSchedule, listJobs } from '@/lib/data/jobs'; import { Button, Card } from '@/components/ui'; import { StatusBadge } from '@/components/status-badge'; import { StatusToggle } from '@/components/status-toggle'; import { setScheduleActive } from '../../actions';
export default async function ScheduleDetailPage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ success?: string }> }) { const { id } = await params; const { success } = await searchParams; const schedule = await getServiceSchedule(id); if (!schedule) notFound(); const upcoming = await listJobs({ from: new Date().toISOString().slice(0, 10), status: 'all' }); const jobs = upcoming.filter((job) => job.service_schedule_id === id).slice(0, 8); return <div className="mx-auto max-w-4xl">{success && <p className="mb-5 rounded bg-teal-50 p-3 text-sm text-teal-800">{success}</p>}<div className="mb-7 flex justify-between"><div><div className="flex gap-3"><h1 className="text-2xl font-semibold">{schedule.name}</h1><StatusBadge isActive={schedule.is_active} /></div><p className="mt-2 text-slate-600">Ab {schedule.valid_from}</p></div><div className="flex gap-3"><Link href={`/dashboard/planung/plaene/${id}/bearbeiten`}><Button variant="outline"><Pencil className="mr-2 size-4" />Bearbeiten</Button></Link><StatusToggle id={id} isActive={schedule.is_active} noun="Plan" action={setScheduleActive} /></div></div><Card className="p-5"><h2 className="font-semibold">Nächste erzeugte Aufträge</h2><div className="mt-4 divide-y">{jobs.length === 0 ? <p className="py-3 text-sm text-slate-600">Noch keine kommenden Aufträge.</p> : jobs.map((job) => <Link className="flex justify-between py-3 text-sm hover:text-teal-700" href={`/dashboard/auftraege/${job.id}`} key={job.id}><span>{job.scheduled_date} · {job.title}</span><span>{job.status}</span></Link>)}</div></Card></div>; }
